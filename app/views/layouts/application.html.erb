<!DOCTYPE html>
<html>
<head>
  <title>Charle Quotes</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<header>
		<div class="row">
			<div class="col-xs-3 logo">
      			<%= link_to root_path, :class => 'logo' do %>
         		 <br />
         		 <h1>Charle Quotes</h1>
             &nbsp;&nbsp;&nbsp;
      			<% end %>
    		</div>

<br/>
<br />

			
	    	<div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links pull-right">
			     <!-- Button trigger modal -->
      			 <%= link_to 'Contribute Quote', '#', :data => {:toggle => 'modal', :target => '#myModal'} %>
      			 &nbsp;&nbsp;&nbsp;
			    <%= link_to 'About', about_path %>
   			</div>
    	</div>
	</header>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Contribute Your Charle </h4>
        </div>
         <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
             <%= f.input :saying, :label => "What is your charlie?", :placeholder => "Charle make i tell you something, so so story manking go give you." %>
             <br/>
            <%= f.input :author, :label => "author of this charlie?", :placeholder => 'Seth' %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Add Charlie', :class => 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>


<hr />
<br class="clear" />

<div class="container">
	<% if flash[:error].present? %>
      <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
        <%= flash[:error].html_safe %>
      </div>
    <% end %>
    <%= yield %>
  </div>

  <div class="footer">
    <div class="container">
      <div class="col-xs-12 col-sm-6">
        <%= link_to "Charle more Charle", root_path, :class => 'next-quote' %>
      </div>
      <div class="col-xs-12 col-sm-6 footer-blurb">
        Proudly hacked in Conway, AR by Seth</a>
        
      </div>
    </div>
  </div>

</body>
</html>
